# @package _global_

defaults:
  - _self_
  - data: flower102 # choose datamodule with `test_dataloader()` for evaluation
  - model: flower102
  - logger: null
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
